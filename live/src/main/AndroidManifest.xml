<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.horen.live">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity android:name=".ui.activity.LiveAnchorCollectActivity" />
        <activity android:name=".ui.activity.LiveCollectActivity" />
        <activity android:name=".ui.activity.LiveDetailActivity" />
        <activity android:name=".ui.activity.VideoActivity" />
        <activity android:name=".ui.activity.LivePlayActivity" />
    </application>
</manifest>
